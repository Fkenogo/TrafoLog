export function SettingsPage() {
  return (
    <section className="panel">
      <div className="panel-heading">
        <h2>Settings</h2>
        <span>Placeholder</span>
      </div>
      <div className="placeholder-copy">
        Settings are intentionally limited in this foundation phase. User, admin, analytics, export, audit, QR, and geo workflows are not exposed.
      </div>
    </section>
  );
}
